import { ExtensionPipePipe } from './extension-pipe.pipe';

describe('ExtensionPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ExtensionPipePipe();
    expect(pipe).toBeTruthy();
  });
});
